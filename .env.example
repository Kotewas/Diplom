DB_NAME=diplom_bd
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

BACKEND_PORT=8080
FRONTEND_PORT=5173

# OpenWeather API key for backend weather endpoint.
WEATHER_API_KEY=

# Comma-separated Spring CORS origin patterns. Use * to allow all frontend URLs.
APP_CORS_ALLOWED_ORIGINS=*
