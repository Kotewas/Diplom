# Optional. By default frontend calls /api and uses Vite proxy to http://localhost:8080 in dev mode.
# If you want to call backend by absolute URL, uncomment:
# VITE_API_BASE_URL=http://localhost:8080
